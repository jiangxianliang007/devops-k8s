apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dex-mysql-pv-claim
  namespace: ckb-dex
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2
  resources:
    requests:
      storage: 30Gi
