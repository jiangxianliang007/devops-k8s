---
apiVersion: v1
kind: Service
metadata:
  name: testnet-redis-svc
  namespace: layer2
  labels:
    app: testnet-redis
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
  selector:
    app: testnet-redis
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: testnet-redis
  namespace: layer2
spec:
  serviceName: testnet-redis
  replicas: 1
  selector:
    matchLabels:
      app: testnet-redis
  template:
    metadata:
      labels:
        app: testnet-redis
    spec:
      containers:
        - name: testnet-redis
          image: redis:6.2
          command:
            - "sh"
            - "-c"
            - "redis-server /usr/local/etc/redis/redis.conf"
          ports:
            - containerPort: 6379
          resources:
            limits:
              cpu: 4000m
              memory: 6144Mi
            requests:
              cpu: 1000m
              memory: 2024Mi
          volumeMounts:
            - name: data
              mountPath: /data
              subPath: godwoken-testnet-redis
            - name: config
              mountPath: /usr/local/etc/redis/redis.conf
              subPath: redis.conf
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: godwoken-data-pvc
        - name: config
          configMap:
            name: testnet-redis-config
